const fs = require("fs-extra-promise");
const path = require("path");

const loadFunctions = (client, clientDirectory) => {

};

module.exports = (client) => new Promise((resolve, reject) => {
    client.functions = {};
});