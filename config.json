// Use "git update-index --assume-unchanged .\config.json" if you're going to make commits in this repo.
// Otherwise you risk commiting sensitive information.
// Use "git update-index --no-assume-unchanged .\config.json" to be able to commit changes to this file.
{
  "authentication": {
    "bot_token": "insert token here",
    "client_id": "insert client id here"
  },
  "embeds": {
    "defaultColor": "#738BD7",
    "errorColor": "#f74f25" 
  },
  "user": {
    "status": "invisible"
  }
}